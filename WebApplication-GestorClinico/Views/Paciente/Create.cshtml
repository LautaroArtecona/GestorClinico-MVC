@model WebApplication_GestorClinico.Models.Paciente

@{
    ViewData["Title"] = "Alta de Paciente";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3 class="text-dark fw-bold">Nuevo Paciente</h3>
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Ver listado
        </a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-header bg-white py-3 border-bottom">
            <h5 class="m-0 font-weight-bold text-info">
                <i class="bi bi-person-vcard"></i> Ficha del Paciente
            </h5>
        </div>

        <div class="card-body p-4">
            <div class="alert alert-light border-start border-info border-4 mb-4">
                <i class="bi bi-info-circle"></i>
                Al registrar al paciente, se le creará automáticamente un <strong>Usuario</strong> (DNI) y una <strong>Historia Clínica</strong> vacía.
            </div>

            <form asp-action="Create">

                <div asp-validation-summary="All" class="text-danger mb-3"></div>

                <div class="row">
                    <div class="col-md-6">
                        <h6 class="text-muted text-uppercase small mb-3">Información Personal</h6>

                        <div class="form-floating mb-3">
                            <input asp-for="Nombre" class="form-control" placeholder="Nombre" />
                            <label asp-for="Nombre">Nombre</label>
                            <span asp-validation-for="Nombre" class="text-danger small"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="Apellido" class="form-control" placeholder="Apellido" />
                            <label asp-for="Apellido">Apellido</label>
                            <span asp-validation-for="Apellido" class="text-danger small"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="Dni" class="form-control" placeholder="DNI" />
                            <label asp-for="Dni">DNI (Sin puntos)</label>
                            <span asp-validation-for="Dni" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <h6 class="text-muted text-uppercase small mb-3">Contacto y Cobertura</h6>

                        <div class="form-floating mb-3">
                            <input asp-for="Email" class="form-control" placeholder="Email" />
                            <label asp-for="Email">Email de Contacto</label>
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="ObraSocial" class="form-control" placeholder="Obra Social" />
                            <label asp-for="ObraSocial">Obra Social / Prepaga</label>
                            <span asp-validation-for="ObraSocial" class="text-danger small"></span>
                        </div>
                    </div>
                </div>

                <hr class="my-4" />

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-info text-white btn-lg px-5 shadow-sm fw-bold">
                        <i class="bi bi-person-plus-fill"></i> Registrar Paciente
                    </button>
                </div>

            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
